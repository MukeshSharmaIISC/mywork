public class MainTest {
    public static void main(String[] args) {
        Person person = new Person("Alice");
        Address address = new Address("Wonderland", "Rabbit Hole St.");
        Company company = new Company("MadHatter Inc.");

        // Set cross-references (cycles)
        person.setAddress(address);
        person.setCompany(company);

        address.setResident(person);
        company.setOfficeAddress(address);
        company.setContactPerson(person);

        // Place a breakpoint here to inspect 'person', 'address', 'company'
        System.out.println("Setup complete. You can inspect the objects now.");
    }
}
